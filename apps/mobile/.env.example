# Required. Use a backend URL that the Android/iOS device can reach.
# Examples: approved HTTPS tunnel, staging HTTPS URL, or emulator/LAN URL for local QA.
# Do not commit private tunnel URLs, credentials, bearer tokens, or secrets.
EXPO_PUBLIC_API_BASE_URL=https://your-approved-backend-url.example.com
